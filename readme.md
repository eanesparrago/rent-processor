Assume date parameters are in ISO format so no special date handling is performed.

TODOs:

- Handle logic for calculating the last rent amount (should be deducted based on the rent end date)
- JSDocs
- Add readme